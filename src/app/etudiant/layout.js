export default function EtudiantLayout({ children }) {
    return (
        <html lang="fr">
            <body>
                {/* Pas de Navigation ici */}
                {children}
            </body>
        </html>
    );
}
